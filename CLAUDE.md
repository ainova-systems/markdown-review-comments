# CLAUDE.md

Read [AGENTS.md](AGENTS.md) — it covers what this extension is, the build and test
commands, the `src/` architecture, the `vscode`-free `MarkdownService` invariant, and the
commit convention.
